{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "gecko-version-resolution.fullname" . }}
  labels:
    {{- include "gecko-version-resolution.labels" . | nindent 4 }}
rules:
  - apiGroups: ["gcp.managed.openshift.io"]
    resources: ["clusters"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["gcp.managed.openshift.io"]
    resources: ["clusters/status"]
    verbs: ["get", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "gecko-version-resolution.fullname" . }}
  labels:
    {{- include "gecko-version-resolution.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "gecko-version-resolution.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "gecko-version-resolution.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
